function mudarCor(elemento, cor){
   elemento.style.color = cor;
}

function onmouseOut (elemento, cor){
   elemento.style.color = cor;
}

function clicarBotao(){
   alert("Botao clicado!");
}

function campoEmFoco(elemento, mensagem){
   elemento.value = mensagem;
}

function campoForaDeFoco(elemento, mensagem){
   elemento.value = mensagem;
}

function onMouseOver(elemento, imagem){
   elemento.src = imagem;
}

function onMouseOut(elemento, imagem){
   elemento.src = imagem;
}